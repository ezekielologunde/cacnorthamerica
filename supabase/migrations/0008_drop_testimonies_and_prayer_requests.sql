-- CACNA is the North America regional/corporate body, not a single
-- congregation -- personal-engagement features (Testimonies, Prayer
-- request submission) don't fit and their public pages/admin screens
-- have been removed. The corporate CACNAGWA prayer line (dial-in info)
-- stays -- it's a real regional program, not this per-person feature.
drop table if exists public.testimonies;
drop table if exists public.prayer_requests;
